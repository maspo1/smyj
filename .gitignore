# --- Dependencies (의존성) ---
node_modules/
dist/
build/
.pnp
.pnp.js

# --- Environment Variables & Secrets (환경 변수 및 보안) ---
# Merriam-Webster: 'Secret' - something kept hidden or unexplained
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.private.json
# Firebase 서비스 계정 키 파일 (보통 json으로 다운로드됨)
serviceAccountKey.json

# --- Firebase Specific (파이어베이스 관련) ---
# 로컬 테스트용 로그 및 캐시
.firebase/
firebug.log
firebase-debug.log*
ui-debug.log*

# --- OS & Editor Files (운영체제 및 에디터 설정) ---
# 팀 프로젝트 시 개별 에디터 설정은 올리지 않는 것이 원칙
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- Logs & Temp (로그 및 임시 파일) ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log